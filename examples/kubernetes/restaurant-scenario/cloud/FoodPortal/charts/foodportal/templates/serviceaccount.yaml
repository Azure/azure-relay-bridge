{{- if .Values.serviceAccount -}}
  {{- if .Values.serviceAccount.create -}}
  apiVersion: v1
  kind: ServiceAccount
  metadata:
    name: {{ include "foodportal.serviceAccountName" . }}
    labels:
  {{ include "foodportal.labels" . | nindent 4 }}
  {{- end -}}
{{- end -}}
