apiVersion: v1
kind: Service
metadata:
  name: {{ include "FoodPortal.fullname" . }}
  labels:
    {{- include "FoodPortal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "FoodPortal.selectorLabels" . | nindent 4 }}
