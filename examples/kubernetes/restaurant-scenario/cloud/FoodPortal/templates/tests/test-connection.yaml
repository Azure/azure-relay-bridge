apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "FoodPortal.fullname" . }}-test-connection"
  labels:
{{ include "FoodPortal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "FoodPortal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
